/**
 * Divide elements into different buckets of fixed size. Sort within the buckets. Then combine all the sorted elements
 * from the buckets into place.
 */
package com.kinmanlui.code;

import com.kinmanlui.main.Resource;

public class BucketSort implements Runnable {

    @Override
    public void run() {
        Integer[] numbers = Resource.INSTANCE.getRandomNumbers();



    }
}
